FILE(GLOB_RECURSE CPP_FILES ./ *.cpp)
ADD_LIBRARY(calimerocore SHARED ${CPP_FILES})
TARGET_LINK_LIBRARIES(calimerocore ${QT_LIBRARIES} ${Boost_SIGNALS_LIBRARY} ${Boost_DATE_TIME_LIBRARY})
SET_TARGET_PROPERTIES(calimerocore
PROPERTIES
OUTPUT_NAME "calimerocore"
PREFIX ""
)

INSTALL(TARGETS calimerocore
        RUNTIME DESTINATION bin
        LIBRARY DESTINATION lib)
